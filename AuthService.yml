spring:
  datasource:
    url: jdbc:mysql://localhost:3306/AuthServiceDatabase
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
      
app:
  jwt:
    secret: QWRyaW5lZWxfU2FoYV8xOC0xMC0yMDAx
